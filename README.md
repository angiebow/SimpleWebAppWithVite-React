# Calculator App

A simple calculator built with React and Vite, using [mathjs](https://mathjs.org/) to evaluate expressions.

## Getting Started

```bash
npm install
npm run dev
```

## Scripts

- `npm run dev` — start the dev server
- `npm run build` — build for production
- `npm run preview` — preview the production build
- `npm run lint` — run ESLint
